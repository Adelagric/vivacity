[package]
name = "vivacity-resolver"
description = "Port of Composer's dependency resolver — semver, Packagist metadata, pool, solver, lock file — for vivacity"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
readme = "README.md"
keywords = ["composer", "php", "resolver", "semver"]
categories = ["development-tools", "command-line-utilities"]

[features]
# Passthrough to vivacity-core's TLS backend selection; see vivacity-core.
default = ["rustls-tls-ring"]
rustls-tls-ring = ["vivacity-core/rustls-tls-ring"]
rustls-tls-no-provider = ["vivacity-core/rustls-tls-no-provider"]

[dependencies]
vivacity-core = { path = "../vivacity-core", version = "0.17.0", default-features = false }
serde.workspace = true
serde_json.workspace = true
thiserror.workspace = true
pcre2.workspace = true
sha1.workspace = true
tempfile.workspace = true

[dev-dependencies]
tempfile.workspace = true
