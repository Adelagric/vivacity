Lock written by Composer 2.10.3 on 2026-09-23 from this composer.json
(Packagist). composer/package-versions-deprecated 1.11.99.5 is the version
every corpus entry locks; the root carries two `replace` entries (one
`self.version`) because the generated map ends with them and the root.
