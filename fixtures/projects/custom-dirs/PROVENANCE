Lock written by Composer 2.10.3 on 2026-09-22 from this composer.json
(Packagist). mnsami/composer-custom-directory-installer 2.0.0 is the
version akaunting locks; two mapped library packages (one through the
`{$vendor}-{$name}` template), one unmapped.
