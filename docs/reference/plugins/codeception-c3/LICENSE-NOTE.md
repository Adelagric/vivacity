codeception/c3 2.9.0 (https://github.com/Codeception/c3) is published under
the MIT license (`"license": "MIT"` in its composer.json; the repository
ships no LICENSE file at this commit, e23298a). `Installer.php` is the
plugin's whole source, vendored here as the reference of
`vivacity-core::c3_plugin`; `c3.php` itself is not vendored — vivacity
copies the one installed under vendor/, as the plugin does.
